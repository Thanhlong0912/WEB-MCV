@if (Session["Admin"] != null)
{
    <li class="dropdown user-dropdown">
        <a href="#" class="dropdown-toggle" data-toggle="dropdown"><i class="fa fa-user"></i> Xin Chào, @Session["Admin"].ToString()!<b class="caret"></b></a>
        <ul class="dropdown-menu">
            <li><a href="@Url.Action("DangXuat", "Admin")"><i class="fa fa-power-off"></i>Đăng Xuất</a></li>
        </ul>
    </li>
}